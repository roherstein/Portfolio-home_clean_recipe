<div class="container">
  <div class="row">
    <% @category.each do |category| %>
      <div class="col-2">
        <%= link_to posts_path(category_id: category.id) do %>
          <%= category.category_name %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>