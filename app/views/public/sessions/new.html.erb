<div class="container">
  <div class='login-flash-message'>
    <%= flash[:notice] %>
    <%= flash[:alert] %>
  </div>
  <div class="row justify-content-center">
    <%= form_with model: User, url: user_session_path do |f| %>
      <div class="col-12 login-field">
        <%= f.label :email,"メールアドレス" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>
      <div class="col-12 login-field">
        <%= f.label :password,"パスワード" %><br />
        <%= f.password_field :password, autocomplete: "current-password" %>
      </div>
      <% if devise_mapping.rememberable? %>
        <div class="col-12 login-field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me,"保存する" %>
        </div>
      <% end %>
      <div class="col-12 login-field d-flex justify-content-center align-items-center">
        <%= f.submit "ログイン", class: "btn btn-success" %>
      </div>
    <% end %>
    <div class="col-12 login-field d-flex justify-content-center align-items-center">
      まだユーザ登録をされていない方は、こちらからご登録ください。
    </div>
    <div class="col-12 login-field d-flex justify-content-center align-items-center">
   　　<%= link_to "ユーザ登録はこちら", new_user_registration_path, class: "btn btn-success"  %>
   　　<%#= render "public/shared/links" %>
    </div>
  </div>

</div>